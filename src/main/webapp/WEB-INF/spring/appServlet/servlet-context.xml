<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:mvc="http://www.springframework.org/schema/mvc"
	xmlns:tx="http://www.springframework.org/schema/tx"
	xsi:schemaLocation="
           http://www.springframework.org/schema/beans
           https://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/context
           https://www.springframework.org/schema/context/spring-context.xsd
           http://www.springframework.org/schema/mvc
           https://www.springframework.org/schema/mvc/spring-mvc.xsd
           http://www.springframework.org/schema/tx
           http://www.springframework.org/schema/tx/spring-tx.xsd" >

	<!-- ========= MVC 기본 설정 ================================ -->

	<!-- @Controller, @RestController, @RequestMapping 스캔 -->
	<context:component-scan
		base-package="com.springmvc" />

	<!-- Spring MVC 기능 활성화 : Handler-Mapping, Handler-Adapter 객체를 생성 -->
	<mvc:annotation-driven />

	<!-- 정적 리소스 경로 매핑 (js, css등) -->
	<mvc:resources mapping="/resources/**"
		location="/resources/" />

	<!-- 업로드된 이미지 파일 접근 허용 -->
	<mvc:resources
	    mapping="/uploadedImages/**"
	    location="file:/home/hosting_users/cd01349/uploadedImages/" />
	
	<!-- json 컨버터 사용 -->
	<mvc:annotation-driven />

	<!-- ========= View Resolver (JSP 경로 설정) ======================== -->
	<bean
		class="org.springframework.web.servlet.view.InternalResourceViewResolver">
		<property name="prefix" value="/WEB-INF/views/" />
		<property name="suffix" value=".jsp" />
	</bean>

	<!-- DataSource 설정 -->
	<bean id="dataSource" class="org.springframework.jdbc.datasource.DriverManagerDataSource">
		<property name="driverClassName" value="org.mariadb.jdbc.Driver" />
		<property name="url" value="jdbc:mariadb://cd01349.cafe24.com:3306/cd01349?useUnicode=true&amp;characterEncoding=utf&amp;serverTimezone=Asia/Seoul&amp;allowPublicKeyRetrieval=true"/>
		<property name="username" value="cd01349" />
		<property name="password" value="mari0722!" />
	</bean>

	<!-- JdbcTemplate 등록 -->
	<bean id="jdbcTemplate"
		class="org.springframework.jdbc.core.JdbcTemplate">
		<property name="dataSource" ref="dataSource" />
	</bean>
	<!-- multipartResolver 등록 -->
	<bean id="multipartResolver"
		class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
		<property name="maxUploadSize" value="52428800" />
		<property name="defaultEncoding" value="UTF-8" />
	</bean>
	<!-- @transaction 설정 -->
	<bean id="transactionManager"
		class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
		<property name="dataSource" ref="dataSource" />
	</bean>

	<tx:annotation-driven
		transaction-manager="transactionManager" />

</beans>